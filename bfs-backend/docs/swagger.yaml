definitions:
  backend_internal_domain.CategoryDTO:
    properties:
      id:
        type: string
      isActive:
        type: boolean
      name:
        type: string
    type: object
  backend_internal_domain.ListResponse-backend_internal_domain_ProductDTO:
    properties:
      count:
        type: integer
      items:
        items:
          $ref: '#/definitions/backend_internal_domain.ProductDTO'
        type: array
    type: object
  backend_internal_domain.MenuDTO:
    properties:
      slots:
        items:
          $ref: '#/definitions/backend_internal_domain.MenuSlotDTO'
        type: array
    type: object
  backend_internal_domain.MenuSlotDTO:
    properties:
      id:
        type: string
      menuSlotItems:
        items:
          $ref: '#/definitions/backend_internal_domain.MenuSlotItemDTO'
        type: array
      name:
        type: string
      sequence:
        type: integer
    type: object
  backend_internal_domain.MenuSlotItemDTO:
    properties:
      category:
        $ref: '#/definitions/backend_internal_domain.CategoryDTO'
      id:
        type: string
      image:
        type: string
      isActive:
        type: boolean
      name:
        type: string
      priceCents:
        type: integer
      type:
        $ref: '#/definitions/backend_internal_domain.ProductType'
    type: object
  backend_internal_domain.ProductDTO:
    properties:
      category:
        $ref: '#/definitions/backend_internal_domain.CategoryDTO'
      id:
        type: string
      image:
        type: string
      isActive:
        type: boolean
      menu:
        $ref: '#/definitions/backend_internal_domain.MenuDTO'
      name:
        type: string
      priceCents:
        type: integer
      type:
        $ref: '#/definitions/backend_internal_domain.ProductType'
    type: object
  backend_internal_domain.ProductType:
    enum:
    - simple
    - menu
    type: string
    x-enum-varnames:
    - ProductTypeSimple
    - ProductTypeMenu
  backend_internal_response.Ack:
    properties:
      message:
        type: string
    type: object
  backend_internal_response.ProblemDetails:
    properties:
      detail:
        type: string
      errors:
        items:
          $ref: '#/definitions/backend_internal_response.ValidationError'
        type: array
      instance:
        type: string
      status:
        type: integer
      title:
        type: string
      type:
        type: string
    type: object
  backend_internal_response.ValidationError:
    properties:
      field:
        description: JSON Pointer (RFC 6901) to the failing field
        type: string
      message:
        description: Human-readable error message
        type: string
      value: {}
    type: object
info:
  contact: {}
  description: Internal BlessThun Food System API
  title: Bless2n Food System API
  version: "1.0"
paths:
  /v1/orders:
    post:
      consumes:
      - application/json
      description: Create a new order with the provided details
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/backend_internal_response.Ack'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/backend_internal_response.ProblemDetails'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/backend_internal_response.ProblemDetails'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/backend_internal_response.ProblemDetails'
      summary: Create a new order
      tags:
      - orders
  /v1/products:
    get:
      description: List all products with optional category filtering
      parameters:
      - description: Filter by category ID
        in: query
        name: category_id
        type: string
      - default: 50
        description: Limit size
        in: query
        maximum: 100
        minimum: 1
        name: limit
        type: integer
      - default: 0
        description: Offset
        in: query
        minimum: 0
        name: offset
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/backend_internal_domain.ListResponse-backend_internal_domain_ProductDTO'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/backend_internal_response.ProblemDetails'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/backend_internal_response.ProblemDetails'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/backend_internal_response.ProblemDetails'
      summary: List products
      tags:
      - products
schemes:
- http
- https
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"

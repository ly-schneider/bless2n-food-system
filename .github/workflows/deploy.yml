name: Deploy

on:
  push:
    branches: [ staging, production ]

permissions:
  contents: read
  packages: write
  id-token: write

env:
  REGISTRY: ghcr.io
  IMAGE_TAG: ${{ github.sha }}

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      web: ${{ steps.filter.outputs.web }}
      cloud: ${{ steps.filter.outputs.cloud }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'bfs-backend/**'
            web:
              - 'bfs-web-app/**'
            cloud:
              - 'bfs-cloud/**'

  deploy-acr:
    name: Deploy ACR Infrastructure
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name }}
    needs: changes
    if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.web == 'true' || needs.changes.outputs.cloud == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Select environment directory
        run: |
          case "${{ github.ref_name }}" in
            staging) echo "ENV_DIR=staging" >> $GITHUB_ENV ;;
            production) echo "ENV_DIR=prod" >> $GITHUB_ENV ;;
            *) echo "Unsupported branch: ${{ github.ref_name }}"; exit 1 ;;
          esac

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Init
        working-directory: bfs-cloud/envs/${{ env.ENV_DIR }}
        run: terraform init -input=false

      - name: Terraform Plan ACR
        working-directory: bfs-cloud/envs/${{ env.ENV_DIR }}
        run: |
          terraform plan -target="module.acr" \
            -var "enable_acr=true" \
            -var "acr_name=${{ vars.ACR_NAME }}" \
            -var "image_tag=${{ github.ref_name }}" \
            -out=acr.tfplan

      - name: Terraform Apply ACR
        working-directory: bfs-cloud/envs/${{ env.ENV_DIR }}
        run: terraform apply -auto-approve acr.tfplan

  build-and-push-images:
    name: Build & Push Images
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name }}
    needs: [changes, deploy-acr]
    if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.web == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Resolve ACR login server
        id: acr
        run: |
          if [ -z "${{ vars.ACR_NAME }}" ]; then
            echo "ACR_NAME not set in environment variables" >&2
            exit 1
          fi
          LOGIN_SERVER=$(az acr show -n "${{ vars.ACR_NAME }}" --query loginServer -o tsv)
          echo "login_server=$LOGIN_SERVER" >> $GITHUB_OUTPUT

      - name: ACR Login
        run: az acr login -n ${{ vars.ACR_NAME }}

      - name: Set Image Names (ACR)
        id: names
        run: |
          echo "backend=${{ steps.acr.outputs.login_server }}/backend" >> $GITHUB_OUTPUT
          echo "frontend=${{ steps.acr.outputs.login_server }}/frontend" >> $GITHUB_OUTPUT

      - name: Build & Push backend image
        if: ${{ needs.changes.outputs.backend == 'true' }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: bfs-backend/Dockerfile
          push: true
          tags: |
            ${{ steps.names.outputs.backend }}:${{ env.IMAGE_TAG }}
            ${{ steps.names.outputs.backend }}:${{ github.ref_name }}
          labels: |
            org.opencontainers.image.source=${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Build & Push frontend image
        if: ${{ needs.changes.outputs.web == 'true' }}
        uses: docker/build-push-action@v6
        with:
          context: bfs-web-app
          file: bfs-web-app/docker/Dockerfile
          build-args: |
            NEXT_PUBLIC_API_BASE_URL=${{ vars.NEXT_PUBLIC_API_BASE_URL }}
            BACKEND_INTERNAL_URL=${{ vars.BACKEND_INTERNAL_URL }}
            NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${{ vars.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
          push: true
          tags: |
            ${{ steps.names.outputs.frontend }}:${{ env.IMAGE_TAG }}
            ${{ steps.names.outputs.frontend }}:${{ github.ref_name }}
          labels: |
            org.opencontainers.image.source=${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

  deploy-infra:
    name: Deploy Container Apps Infrastructure
    needs: [changes, build-and-push-images]
    if: always() && (needs.changes.outputs.backend == 'true' || needs.changes.outputs.web == 'true' || needs.changes.outputs.cloud == 'true')
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Select environment directory
        run: |
          case "${{ github.ref_name }}" in
            staging) echo "ENV_DIR=staging" >> $GITHUB_ENV ;;
            production) echo "ENV_DIR=prod" >> $GITHUB_ENV ;;
            *) echo "Unsupported branch: ${{ github.ref_name }}"; exit 1 ;;
          esac

      - name: Choose image tag
        if: ${{ needs.changes.outputs.backend == 'true' || needs.changes.outputs.web == 'true' }}
        run: echo "IMAGE_TAG=${{ github.sha }}" >> $GITHUB_ENV

      - name: Choose image tag (cloud-only changes)
        if: ${{ needs.changes.outputs.backend != 'true' && needs.changes.outputs.web != 'true' }}
        run: echo "IMAGE_TAG=${{ github.ref_name }}" >> $GITHUB_ENV

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Init
        working-directory: bfs-cloud/envs/${{ env.ENV_DIR }}
        run: terraform init -input=false

      - name: Terraform Apply
        working-directory: bfs-cloud/envs/${{ env.ENV_DIR }}
        run: |
          terraform apply -auto-approve -input=false \
            -var "enable_acr=true" \
            -var "acr_name=${{ vars.ACR_NAME }}" \
            -var "image_tag=${{ env.IMAGE_TAG }}"

concurrency:
  group: deploy-${{ github.ref_name }}
  cancel-in-progress: false

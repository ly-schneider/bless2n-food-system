/**
 * Server-side auth exports for API routes and server components.
 *
 * Re-exports from the root lib/auth.ts for backwards compatibility.
 * For client-side usage, import from "@/lib/auth-client" instead.
 */
export { auth, authHandler, authApi, type Auth } from "../auth"
